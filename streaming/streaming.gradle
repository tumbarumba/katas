apply plugin: 'application'
mainClassName = "com.exubero.streaming.WordCount"

dependencies {
    implementation libs.flinkStreamingJava
}

jar.manifest {
    attributes('Main-Class': mainClassName)
}